## 1. 核心功能

+ 调用析构：它会调用给定指针指向的对象的析构函数

+ 不释放内存：它仅仅销毁对象，底层的内存块仍然保留，需要用户手动管理（例如后续调用 operator delete 或重用该内存）。

+ 适用范围：

  + 单个对象 (```std::destroy(pointer)```)

  + 范围对象 (```std::destroy(first, last)```)

  + 指定数量的对象 (```std::destroy_n(first, count)```)
