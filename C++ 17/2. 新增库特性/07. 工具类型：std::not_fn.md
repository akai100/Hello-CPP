## 1. 核心功能

它的作用是对一个可调用对象（函数、Lambda、函数对象等）进行逻辑取反。

+ 如果原可调用对象返回 ```true```，```std::not_fn``` 生成的新对象返回 ```false```。

+ 如果原可调用对象返回 ```false```，新对象返回 ```true```。

它是 C++14 及以前版本中 ```std::not1``` 和 ```std::not2``` 的现代化替代品。与旧版本相比，```std::not_fn``` 更加通用、灵活，且支持完美转发和任意参数数量的可调用对象。
